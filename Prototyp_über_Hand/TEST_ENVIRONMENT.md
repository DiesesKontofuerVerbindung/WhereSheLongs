# Hand Checkbox Prototype — Test Environment

填写一次 Run 对应的一份环境说明；自动生成的 `environment.json` 和 `config_snapshot.json` 是机器可读记录，本文件补足无法可靠自动取得的信息。

```text
Device:
Webcam:

Camera resolution:
Camera FPS:

Window resolution:

Lighting condition:
Camera-to-user distance:
Hand used:
Approximate hand position:

Checkbox radius:
Checkbox screen position:

Positive trials:
Negative trials:

Git commit:
Date:
Notes:
```

建议每次测试结束，把本文件复制到对应 `results/run_*/` 目录并填写。不要把“感觉今天很稳”写成数据，Danke.
