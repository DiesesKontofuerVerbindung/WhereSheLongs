# Wedding ownership

This directory is owned by the Wedding programmer.

Use `res://shared/player/player.tscn` instead of creating a separate Player.
Wedding/rehearsal/ceremony-specific systems stay here unless a shared reuse case is explicitly approved.

Scene-local tests belong under `scenes/wedding/tests/**` and are Wedding-owned. Root `tests/**` remains protected shared infrastructure.
